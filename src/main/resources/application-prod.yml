spring:
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    pl.tomaszosuch.trainingplatform_backend: INFO
    org.springframework.security: WARN

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
app:
  refresh-token:
    cookie-secure: true
    cookie-same-site: Lax