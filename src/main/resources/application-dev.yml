spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    pl.tomaszosuch.trainingplatform_backend: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG